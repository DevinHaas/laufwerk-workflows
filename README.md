# Shared Laufwerk workflows

Reusable Laufwerk workflows, components, and activities shared by consumer projects.

Consumer projects keep local discovery shims under `laufwerk/workflows/` because the
current CLI discovers workflow files from that directory.
